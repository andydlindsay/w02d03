<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Dogs</title>
</head>
<body>
    <h1>All of our dogs</h1>

    <% if (dogs) { %>
        <ul>
            <% for (const key in dogs) { %>
                <li><h3><%= `${key} - ${dogs[key]}` %></h3></li>
            <% } %>
        </ul>
    <% } %>
</body>
</html>